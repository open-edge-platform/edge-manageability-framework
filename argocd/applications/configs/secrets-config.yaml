# SPDX-FileCopyrightText: 2025 Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

auth:
  orchSvcs:
    roleMaxTTL: 1h
  oidc:
    # Use fully-qualified cluster DNS name to avoid any lookup ambiguity
    # Use the actual HTTP service name created by the Keycloak chart
    idPAddr: "http://platform-keycloak.orch-platform.svc.cluster.local:8080"
    idPDiscoveryURL: "http://platform-keycloak.orch-platform.svc.cluster.local:8080/realms/master"
    roleMaxTTL: 1h
